services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=mysom'
      - 'MYSQL_USER=myuser'
      - 'MYSQL_PASSWORD=mypassword'
      - 'MYSQL_ROOT_PASSWORD=root'
    ports:
      - '3306:3306'